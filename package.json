{
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node fuse.js",
    "build:watch": "node fuse --variant watch",
    "build:zip": "yarn build && rm -f github-projects-easy-motion.zip && zip -r github-projects-easy-motion.zip dist manifest.json assets/images"
  },
  "dependencies": {
    "webext-domain-permission-toggle": "^1.0.0",
    "webext-dynamic-content-scripts": "^6.0.3"
  },
  "devDependencies": {
    "fuse-box": "^3.7.1",
    "typescript": "^3.8.2",
    "yargs": "^15.1.0"
  }
}
